int q;
int main() {
	int i;
        int p,n;
        int k,j;
        int m;
	for (q=0; i<9; i=i*1+1/2) {
	}
        for (i=0, p=4+4; i<9; i=i+1, p=i) {
            write("test");
        }
        for (;;) {
        }
        while (1) {
        }
	q=2;
        while (q==2) {
            write(2);
            write("wow");
        }
        if (k == j) {
            write("first");
        }
        if (k == j) {
            write("first");
        } else if (j == k) {
            write("second");
        } else {
            write("third");
        }
        
}
